model User {
    id       String  @id @default(uuid())
    email    String  @unique
    password String  @db.VarChar(255)
    name     String? @db.VarChar(255)
    age      Int?    @db.SmallInt()
    posts    Post[]

    @@map("users")
}
